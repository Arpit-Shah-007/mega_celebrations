# Internal planning docs, client media exports, and session notes — not for the repo
# (this also covers docs/r2_storage/, the local used-vs-unused R2 media export)
docs/

# Claude Code project config/session state — local tooling only, never pushed
.claude/

# Local holding area for removed-but-not-yet-deleted files
trash/

# Wrangler local cache/state
.wrangler/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# Node (in case anything runs at repo root)
node_modules/

# Env files — never commit secrets
.env
.env.local
.env.*.local
.dev.vars
